import { AUTH } from "./auth.endpoints";
import { CATEGORY } from "./category.endpoints";
import { COMMENT } from "./comments.endpoints";
import { POST } from "./posts.endpoint";
import { USER } from "./user.endpoints";

export default {
  AUTH,
  POST,
  USER,
  COMMENT,
  CATEGORY,
}